# 값은 커밋하지 않는다. 실제 값은 .env.local(gitignore 대상)에 넣는다.

# 클라이언트 노출 허용
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 서버 전용 — NEXT_PUBLIC_ 접두사 금지
SUPABASE_SERVICE_ROLE_KEY=
OG_PARSER_URL=
# og-parser의 X-API-Key와 같은 값 (og-parser/README.md 참고)
OG_PARSER_API_KEY=
